######################
## SCRIPTED EFFECTS ##
######################

#### STATE SCOPE ####
retain_state = {
	set_state_flag = { flag = retain_state_@ROOT days = 16 value = 1 }
	set_temp_variable = { retain_state_@THIS = 1 }
}

clear_retained_states = {
	hidden_effect = { every_state = { clear_retain_state = yes } }
}

clear_retain_state = {
	clr_state_flag = retain_state_@PREV
	set_temp_variable = { retain_state_@THIS = 0 } #clear_variable doesn't work for temp variables
}

occupy_state = {
	set_state_flag = occupied_by_@PREV
	set_temp_variable = { occupied_by_@THIS = 1 }
}

clear_occupy_state = {
	clr_state_flag = occupied_by_@PREV
	set_temp_variable = { occupied_by_@THIS = 0 } #clear_variable doesn't work for temp variables
}

transfer_state_without_overriding_occupation = {
	save_current_factory_count = yes
	if = {
		limit = { is_fully_controlled_by = owner }
		transfer_state_to = var:tag_to_release
	}
	else = {
		set_state_owner_to = var:tag_to_release
		set_state_province_controller = {
			controller = var:tag_to_release
			limit = { NOT = { has_war_with = ROOT } }
		}
	}
	restore_previous_factory_count = yes
	if = {
		limit = { is_claim_or_core_of_target = no }
		var:tag_to_release = { PREV = { set_state_flag = { flag = annexation_recent_transfer_@PREV value = 1 days = 60 } } }
	}
}

# state scope
state_given_notification_effect = {
	hidden_effect = {
		if = {
			limit = { owner = { is_ai = no } }
			save_event_target_as = granted_land
			state_event = { id = annex.2 trigger_for = owner }
		}
	}
}

# state scope
transfer_state_and_notify = {
	transfer_state_without_overriding_occupation = yes
	state_given_notification_effect = yes
}

# state scope
transfer_if_owned = {
	if = {
		limit = { is_valid_annexation_state = yes }
		transfer_state_and_notify = yes
	}
}

# state scope
add_claim_of_ROOT = {
	effect_tooltip = {
		if = {
			limit = { is_claim_or_core_of_ROOT = no }
			add_claim_by = ROOT
		}
	}
	hidden_effect = {
		if = {
			limit = { NOT = { is_core_of = ROOT } }
			add_claim_by = ROOT
		}
	}
}

# state scope
remove_claim_and_add_core_of_target = {
	effect_tooltip = {
		if = {
			limit = { is_claimed_by = var:tag_to_release }
			remove_claim_by = var:tag_to_release
		}
		if = {
			limit = { NOT = { is_core_of = var:tag_to_release } }
			add_core_of = var:tag_to_release
		}
	}
	hidden_effect = {
		remove_claim_by = var:tag_to_release
		add_core_of = var:tag_to_release
	}
}

# state scope
set_transfer_target_state = {
	set_temp_variable = { state_transfer_target_@THIS = 1 }
}

change_transfer_flag_target = {
	owner = { PREV = { clr_state_flag = annexation_recent_transfer_@PREV } }
	var:tag_to_release = { PREV = { set_state_flag = { flag = annexation_recent_transfer_@PREV value = 1 days = 60 } } }
}

# state scope
remove_claim_and_add_core_of_ROOT = {
	effect_tooltip = {
		if = {
			limit = { is_claimed_by = ROOT }
			remove_claim_by = ROOT
		}
		if = {
			limit = { NOT = { is_core_of = ROOT } }
			add_core_of = ROOT
		}
	}
	hidden_effect = {
		remove_claim_by = ROOT
		add_core_of = ROOT
	}
}

# state scope
add_claim_of_target = {
	effect_tooltip = {
		if = {
			limit = { is_claim_or_core_of_target = no }
			add_claim_by = var:tag_to_release
		}
	}
	hidden_effect = {
		if = {
			limit = { NOT = { is_core_of = var:tag_to_release } }
			add_claim_by = var:tag_to_release
		}
	}
}

# country scope
add_claim_of_target_on_valid_cores = {
	every_core_state = {
		limit = {
			is_allowed_annexation_state = yes
			is_claim_or_core_of_target = no
		}
		add_claim_by = var:tag_to_release
	}
}

#### GLOBAL SCOPE ####

# global scope
release_targeted_tag_with_claims = {
	set_temp_variable = { claims = 1 }
	release_targeted_tag_state_transfer_effect = yes
	puppet_released_tag = yes
}

# global scope
release_targeted_tag_with_claims_nocosmetic = {
	set_temp_variable = { claims = 1 }
	release_targeted_tag_state_transfer_effect = yes
	puppet_released_tag_nocosmetic = yes
}

# global scope
release_targeted_tag = {
	release_targeted_tag_state_transfer_effect = yes
	puppet_released_tag = yes
}

# global scope
release_targeted_tag_nocosmetic = {
	release_targeted_tag_state_transfer_effect = yes
	puppet_released_tag_nocosmetic = yes
}

# global scope
puppet_released_tag = {
	puppet_released_tag_nocosmetic = yes
	var:tag_to_release = { drop_cosmetic_tag = yes }
}

# global scope
puppet_released_tag_nocosmetic = {
	puppet = var:tag_to_release
	var:tag_to_release = {
		mark_focus_tree_layout_dirty = yes
		hidden_effect = {
			#transfer_technology_without_doctrines = yes
			remove_ideas = ready_to_surrender
			if = {
				limit = { has_elections = yes }
				set_politics = {
					ruling_party = var:current_party_ideology_group
					elections_allowed = no
				}
			}
			every_possible_country = {
				clear_embargo_PREV = yes
				clear_volunteers_to_PREV = yes
				PREV = { clear_volunteers_to_PREV = yes }
			}
		}
	}
}

# country scope
set_transfer_target_cores = {
	set_temp_variable = { transfer_target_@THIS = 1 }
}

# global scope
transfer_targeted_territory = {
	every_owned_state = {
		limit = {
			OR = {
				has_variable = state_transfer_target_@THIS
				any_country_with_core = { has_variable = transfer_target_@THIS }
			}
			is_allowed_annexation_state = yes
		}
		transfer_state_without_overriding_occupation = yes
		state_given_notification_effect = yes
	}
}

release_targeted_tag_state_transfer_effect = {
	if = {
		limit = { has_variable = claims }
		every_owned_state = {
			limit = {
				is_claim_or_core_of_target = yes
				is_allowed_annexation_state = yes
			}
			transfer_state_without_overriding_occupation = yes
		}
	}
	else = {
		every_owned_state = {
			limit = {
				is_core_of = var:tag_to_release
				is_allowed_annexation_state = yes
			}
			transfer_state_without_overriding_occupation = yes
		}
	}
}

# country scope
annex_splinter_tag = {
	var:tag_to_release = {
		every_owned_state = { change_transfer_flag_target = yes }
		annex_country = { target = PREV transfer_troops = yes }
	}
}

# country scope
add_claim_of_ROOT_on_valid_cores = {
	every_core_state = {
		limit = {
			is_allowed_annexation_state = yes
			is_claim_or_core_of_ROOT = no
		}
		add_claim_by = ROOT
	}
}

#################
## GUI EFFECTS ##
#################

recheck_annexations_base = {
	#clear_array = offered_states_array (In consideration / omitted for further use)
	allow_annexation_decision_selection = yes
}

recheck_annexations_immediate = {
	recheck_annexations_base = yes
	hidden_effect = {
		country_event = annex.1
	}
}

recheck_annexations = {
	recheck_annexations_base = yes
	hidden_effect = {
		country_event = { id = annex.1 days = 1 random_days = 3 }
	}
}

allow_annexation_decision_selection = {
	clr_country_flag = open_annexation_event
	clear_retained_states = yes
}

build_annexation_temp_arrays = {
	if = {
		limit = { has_variable = annexation_extra_states^num }
		if = {
			limit = { var:annexation_target = { is_valid_annexation_state = yes } }
			set_temp_variable = { priority_state_@var:annexation_target = annexation_target }
			for_each_scope_loop = {
				array = annexation_extra_states
				if = {
					limit = {
						is_allowed_annexation_state_for_owner = yes
						impassable = no
					}

					### build owners array
					if = {
						limit = { NOT = { is_in_array = { owners_@PREV = owner } } }
						add_to_temp_array = { owners_@PREV = owner }
					}

					### add up total owned strategic value
					add_to_temp_variable = { total_score_@var:owner = state_strategic_value }
				}
			}
		}
		else = {
			for_each_scope_loop = {
				array = annexation_extra_states
				if = {
					limit = {
						is_allowed_annexation_state_for_owner = yes
						impassable = no
					}

					### build owners array
					if = {
						limit = { NOT = { is_in_array = { owners_@PREV = owner } } }
						add_to_temp_array = { owners_@PREV = owner }
					}

					### add up total owned strategic value
					add_to_temp_variable = { total_score_@var:owner = state_strategic_value }

					### select best state to target the decision
					if = {
						limit = {
							is_owned_by = ROOT
							check_variable = { annexation_target = PREV.annexation_target }
						}
						set_temp_variable = { has_valid_state_@var:annexation_target = THIS }
						if = {
							limit = { NOT = { has_variable = priority_state_@var:annexation_target } }
							set_temp_variable = { priority_state_@var:annexation_target = THIS }
						}
						else_if = {
							limit = { var:priority_state_@var:annexation_target = { check_variable = { state_strategic_value < PREV.state_strategic_value } } }
							set_temp_variable = { priority_state_@var:annexation_target = THIS }
						}
						else_if = {
							limit = { var:priority_state_@var:annexation_target = { check_variable = { id > PREV.id } } }
							set_temp_variable = { priority_state_@var:annexation_target = THIS }
						}
					}
				}
			}
		}

		### assign total owned strategic value to each owner
		for_each_loop = {
			array = owners_@THIS
			add_to_temp_array = { owner_scores_@THIS = total_score_@var:v }
		}

		### select country with the highest total owned strategic value
		find_highest_in_array = { array = owner_scores_@THIS }
		set_temp_variable = { priority_country_@var:annexation_target = owners_@THIS^i }
	}
}

block_annexation_decision_selection = {
	clear_retained_states = yes
	FROM = {
		if = {
			limit = { has_variable = annexation_target }
			var:annexation_target = { save_event_target_as = targeted_annexation_state }
		}
		else = {
			save_event_target_as = targeted_annexation_state
		}
	}
	set_country_flag = { flag = open_annexation_event days = 16 value = 1 }
}

initialise_regional_annexation_variables = {
	add_to_regional_annexation_array = yes
	set_state_as_annexation_target = yes
}

set_state_as_annexation_target = {
	if = {
		limit = { NOT = { has_variable = PREV.annexation_target } }
		set_variable = { PREV.annexation_target = THIS }
	}
}

add_to_regional_annexation_array = {
	if = {
		limit = { NOT = { is_in_array = { annexation_extra_states = PREV } } }
		add_to_array = { annexation_extra_states = PREV }
	}
}

annexation_decision_effect = {
	clear_retained_states = yes
	FROM = {
		if = {
			limit = { has_variable = annexation_target }
			var:annexation_target = { save_event_target_as = targeted_annexation_state }
		}
		else = {
			save_event_target_as = targeted_annexation_state
		}
	}
	set_country_flag = { flag = open_annexation_event days = 16 value = 1 }
	### Temporary Only (See below note) ###
	meta_effect = {
		text = { country_event = [EVENT] }
		EVENT = "annex.[?FROM.annexation_target:annx_evnt_num]"
	}
	# if = { 
		## Note: Remove when the annexation event for islands has implemented (uses Temporary Only for now)
	# 	limit = {
	# 		OR = { #Islands' Annexation List
	# 			check_variable = { FROM.annexation_target:annx_evnt_num = 2057 }
	# 			check_variable = { FROM.annexation_target:annx_evnt_num = 3076 }
	# 			check_variable = { FROM.annexation_target:annx_evnt_num = 3075 }
	# 			check_variable = { FROM.annexation_target:annx_evnt_num = 3074 }
	# 			check_variable = { FROM.annexation_target:annx_evnt_num = 3019 }
	# 			check_variable = { FROM.annexation_target:annx_evnt_num = 3083 }
	# 			check_variable = { FROM.annexation_target:annx_evnt_num = 3078 }
	# 			check_variable = { FROM.annexation_target:annx_evnt_num = 3071 }
	# 			check_variable = { FROM.annexation_target:annx_evnt_num = 3067 }
	# 			check_variable = { FROM.annexation_target:annx_evnt_num = 5052 }
	# 			check_variable = { FROM.annexation_target:annx_evnt_num = 5054 }
	# 		}
	# 	}
	# 	FROM = {
	# 		meta_effect = {
	# 			text = { state_event = { id = [EVENT] trigger_for = ROOT } }
	# 			EVENT = "annex.[?annexation_target:annx_evnt_num]"
	# 		}
	# 	}
	# }
	# else = {
	# 	meta_effect = {
	# 		text = { country_event = [EVENT] }
	# 		EVENT = "annex.[?FROM.annexation_target:annx_evnt_num]"
	# 	}
	# }
}

#################################
## ANNEXATION POSSIBLE RELEASE ##
#################################

check_possible_annexations = {
	if = {
		limit = {
			meta_trigger = {
				text = { can_release_[STATE] = yes }
				STATE = "[?THIS.annexation_target.GetID]"
			}
		}
		ROOT = {
			add_to_variable = { active_annexation_decisions = 1 }
			activate_targeted_decision = { decision = annexation_decision target = PREV }
		}
	}
}

### AI Annexation Release ###
annexations_create_non_socialist_britain_event_target = {
	if = {
		limit = { is_subject = no }
		if = {
			limit = {
				var:6.owner = { #Greater London
					annexations_is_valid_britain = yes
					is_ally_of_ROOT = yes
					has_socialist_government = no
				}
			}
			var:6.owner = {
				save_event_target_as = britain
				add_to_array = { ROOT.potential_grant_targets = THIS }
			}
		}
	}
}

annexations_reset_belgian_tags = {
	if = {
		limit = { NOT = { country_exists = BEL } }
		every_state = {
			if = {
				limit = { is_belgium = yes }
				add_core_of = BEL
			}
			else = {
				remove_core_of = BEL
			}
			remove_claim_by = BEL
		}
		BEL = {
			drop_cosmetic_tag = yes
			# BEL_remove_monarchy = yes
			62 = { disable_state_integration = yes } #Westerscheldt
			64 = { disable_state_integration = yes } #Limburg
			68 = { disable_state_integration = yes } #Lille
		}
	}
	if = {
		limit = { NOT = { country_exists = FLA } }
		FLA = {
			clear_cores_of_tag_if_possible = yes
			drop_cosmetic_tag = yes
			# BEL_remove_monarchy = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = WLL } }
		WLL = {
			clear_cores_of_tag_if_possible = yes
			drop_cosmetic_tag = yes
			# BEL_remove_monarchy = yes
		}
	}
}

annexations_reset_italian_tags = {
	every_state = {
		limit = {
			has_variable = ITA_default_core_state
			check_variable = { core_countries^num = 1 }
		}
		if = {
			limit = { NOT = { country_exists = var:ITA_default_core_state } }
			add_core_of = var:ITA_default_core_state
		}
		else_if = {
			limit = { NOT = { country_exists = ITA } }
			add_core_of = ITA
		}
	}
	if = {
		limit = { NOT = { country_exists = ITA } }
		ITA = {
			# ITA_remove_monarchy = yes
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = STE } }
		STE = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = VNE } }
		VNE = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = LOM } }
		LOM = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = PIE } }
		PIE = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = SAR } }
		SAR = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = SIT } }
		SIT = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
}

annexation_release_italy = {
	every_state = {
		limit = {
			is_european_italy = yes
		}
		if = {
			limit = { is_owned_by = ROOT }
			remove_claim_and_add_core_of_target = yes
		}
		else = {
			add_claim_of_target = yes
		}
	}
	release_targeted_tag = yes
	hidden_effect = {
		for_each_scope_loop = {
			array = global.italian_tags_array
			if = {
				limit = { exists = no }
				clear_cores_of_tag_if_possible = yes
			}
		}
	}
}

annexations_give_italy_to_target = {
	if = {
		limit = { has_event_target = italy }
		set_temp_variable = { tag_to_release = event_target:italy }
		every_owned_state = {
			limit = {
				is_european_italy = yes
				# is_italian_irredentist_claim = no
				is_allowed_annexation_state = yes
			}
			transfer_state_and_notify = yes
		}
		recheck_annexations = yes
	}
}

annexations_reset_german_tags = {
	if = {
		limit = { NOT = { country_exists = GFD } }
		GFD = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = RHI } }
		RHI = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = NGM } }
		NGM = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = PRS } }
		PRS = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = SHO } }
		SHO = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = SGM } }
		SGM = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
}

annexations_create_german_event_targets = {
	if = {
		limit = {
			has_country_flag = is_potential_unifier_GER
			is_subject = no
		}
		random_other_country = {
			limit = {
				has_country_flag = is_potential_unifier_GER
				is_ally_with = ROOT
			}
			save_event_target_as = germany
			add_to_array = { ROOT.potential_grant_targets = THIS }
		}
		if = {
			limit = { NOT = { has_event_target = germany } }
			random_other_country = {
				limit = {
					has_country_flag = is_potential_unifier_GER
					ROOT_can_grant_land = yes
				}
				save_event_target_as = germany
				add_to_array = { ROOT.potential_grant_targets = THIS }
			}
		}
	}
}

annexations_release_germany = {
	if = {
		limit = {
			any_country_with_original_tag = {
				original_tag_to_check = GFD
				is_ally_of_ROOT = yes
				set_temp_variable = { tag_to_release = THIS }
			}
		}
		every_owned_state = {
			limit = {
				is_germany = yes
				is_valid_annexation_state = yes
			}
			transfer_state_and_notify = yes
		}
	}
	else = {
		annexations_germany_retain_special_states = yes
		generate_new_german_puppet = yes
		every_subject_country = {
			limit = {
				is_germany_or_splinter_tag = yes
				NOT = { has_country_flag = is_potential_unifier_GER }
				NOT = { original_tag = RHI }
				NOT = { original_tag = PRS }
				is_ai = yes
			}
			annex_splinter_tag = yes
		}
	}
}

annexations_germany_retain_special_states = {
	hidden_effect = {
		if = {
			limit = { event_target:targeted_annexation_state = { is_rhineland = no } }
			51 = { retain_state = yes } #Western Rhineland
			53 = { retain_state = yes } #Palatinate
			54 = { retain_state = yes } #Eastern Rhineland
		}
	}
}

generate_new_german_puppet = {
	if = {
		limit = { NOT = { country_exists = GFD } }
		set_temp_variable = { tag_to_release = GFD }
		every_state = {
			limit = { is_germany = yes }
			add_core_of = var:tag_to_release
		}
		release_targeted_tag = yes
	}
	else = {
		set_temp_variable = { target = GFD }
		create_dynamic_country = {
			original_tag = GFD
			set_temp_variable = { tag_to_release = THIS }
			ROOT = {
				every_owned_state = {
					limit = { is_core_of = GFD }
					transfer_state_without_overriding_occupation = yes
				}
			}
		}
		puppet_released_tag = yes
	}
}

annexations_reset_yugoslav_tags = {
	if = {
		limit = { NOT = { country_exists = YUG } }
		YUG = {
			drop_cosmetic_tag = yes
			clear_cores_of_tag_if_possible = yes
		}
	}
	if = {
		limit = { NOT = { country_exists = SLV } }
		every_state = {
			limit = { is_slovenia = no }
			remove_core_of = SLV
			remove_claim_by = SLV
		}
		SLV = { drop_cosmetic_tag = yes }
	}
	if = {
		limit = { NOT = { country_exists = CRO } }
		every_state = {
			limit = { is_croatia = no }
			remove_core_of = CRO
			remove_claim_by = CRO
		}
		CRO = { drop_cosmetic_tag = yes }
	}
	if = {
		limit = { NOT = { country_exists = BOS } }
		every_state = {
			limit = { is_bosnia = no }
			remove_core_of = BOS
			remove_claim_by = BOS
		}
		BOS = { drop_cosmetic_tag = yes }
	}
	if = {
		limit = { NOT = { country_exists = SER } }
		every_state = {
			limit = { is_serbia = no }
			remove_core_of = SER
			remove_claim_by = SER
		}
		SER = { drop_cosmetic_tag = yes }
	}
	if = {
		limit = { NOT = { country_exists = MNE } }
		every_state = {
			limit = { is_montenegro = no }
			remove_core_of = MNE
			remove_claim_by = MNE
		}
		MNE = { drop_cosmetic_tag = yes }
	}
	if = {
		limit = { NOT = { country_exists = MAC } }
		every_state = {
			limit = { is_macedonia = no }
			remove_core_of = MAC
			remove_claim_by = MAC
		}
		MAC = { drop_cosmetic_tag = yes }
	}
}