scripted_gui = {
	FH_world_congress_gui = {
		window_name = "FH_world_congress_button_container"
		parent_window_token = politics_tab
		
		ai_enabled = { always = no }

		triggers = {
			world_congress_button_visible = {
				#has_variable = is_in_world_congress
				is_ai = no
			}
		}

		effects = {
			world_congress_button_click = {
				# if = {
				# 	limit = {
				# 		ROOT = { has_country_flag = world_congress_list_open }
				# 	}
				# 	clear_array = ROOT.world_congress_list_array
				# 	ROOT = { clr_country_flag = world_congress_list_open }
				# }
				# else = {
				# 	for_each_loop = {
				# 		array = in_world_congress:members
				# 		add_to_array = { ROOT.world_congress_list_array = v }
				# 	}
				# 	ROOT = { set_country_flag = world_congress_list_open }
				# }
			}
		}

		properties = {
			world_congress_button = {
				image = "GFX_world_factions"
			}
		}
	}
	# economic_sphere_list = {
	# 	context_type = player_context
	# 	window_name = "economic_sphere_list_window"
	# 	dirty = economic_sphere_list_array^num

	# 	ai_enabled = {
	# 		always = no
	# 	}

	# 	visible = {
	# 		has_country_flag = economic_sphere_list_open
	# 	}

	# 	dynamic_lists = {
	# 		economic_sphere_tags_grid = {
	# 			array = economic_sphere_list_array
	# 			change_scope = yes

	# 			entry_container = economic_sphere_member_entry
	# 		}
	# 	}

	# 	triggers = {
	# 		major_country_overlay_visible = {
	# 			tag = ROOT.economic_sphere_list_array^0
	# 		}
	# 	}

	# 	effects = {
	# 		button_close_click = {
	# 			clear_array = ROOT.economic_sphere_list_array
	# 			clr_country_flag = economic_sphere_list_open
	# 		}
	# 	}

	# 	properties = {
	# 		economic_sphere_icon = {
	# 			image = "[?ROOT.economic_sphere_list_array^0.GetEconomicSphereIcon]"
	# 		}
	# 		country_flag = {
	# 			image = "[This.GetFlag]"
	# 		}
	# 	}
	# }
}