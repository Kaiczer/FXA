debug_decisions = {
	check_annexation_SOV = {
		icon = generic_flags

		fire_only_once = no 
		cost = no 

		complete_effect = {
			ROOT = {
				annex_country = { target = SOV transfer_troops = no }
			}
			UKR = { annex_country = { target = USR } }
			BLR = { annex_country = { target = BSR } }
			recheck_annexations = yes
		}
	}
	check_annexation_RUS = {
		icon = generic_flags

		fire_only_once = no 
		cost = no 

		complete_effect = {
			ROOT = {
				annex_country = { target = RUS transfer_troops = no }
			}
			recheck_annexations = yes
		}
	}
	check_annexation_EU = {
		icon = generic_flags

		fire_only_once = no 
		cost = no 

		complete_effect = {
			every_country = {
				limit = { is_in_europe = yes }
				ROOT = { annex_country = { target = PREV } }
			}
			every_state = {
				limit = { 
					NOT = { is_owned_by = ROOT }
					is_on_continent = europe
				}
				transfer_state_to = ROOT
			}
			recheck_annexations = yes
		}
	}
	check_annexation_AF = {
		icon = generic_flags

		fire_only_once = no 
		cost = no 

		complete_effect = {
			every_country = {
				limit = { is_in_africa = yes }
				ROOT = { annex_country = { target = PREV } }
			}
			every_state = {
				limit = { 
					NOT = { is_owned_by = ROOT }
					is_on_continent = africa
				}
				transfer_state_to = ROOT
			}
			recheck_annexations = yes
		}
	}
	check_annexation_IND = {
		icon = generic_flags

		fire_only_once = no 
		cost = no 

		complete_effect = {
			every_country = {
				limit = { is_in_india = yes }
				ROOT = { annex_country = { target = PREV } }
			}
			every_state = {
				limit = { 
					NOT = { is_owned_by = ROOT }
					is_on_continent = india
				}
				transfer_state_to = ROOT
			}
			recheck_annexations = yes
		}
	}
	check_annexation_AS = {
		icon = generic_flags

		fire_only_once = no 
		cost = no 

		complete_effect = {
			every_country = {
				limit = { is_in_asia = yes }
				ROOT = { annex_country = { target = PREV } }
			}
			every_state = {
				limit = { 
					NOT = { is_owned_by = ROOT }
					is_on_continent = asia
				}
				transfer_state_to = ROOT
			}
			recheck_annexations = yes
		}
	}
	check_annexation_ME = {
		icon = generic_flags

		fire_only_once = no 
		cost = no 

		complete_effect = {
			every_country = {
				limit = { is_in_middle_east = yes }
				ROOT = { annex_country = { target = PREV } }
			}
			every_state = {
				limit = { 
					NOT = { is_owned_by = ROOT }
					is_on_continent = middle_east
				}
				transfer_state_to = ROOT
			}
			recheck_annexations = yes
		}
	}
	check_annexation_NAM = {
		icon = generic_flags

		fire_only_once = no 
		cost = no 

		complete_effect = {
			every_country = {
				limit = { is_in_north_america = yes }
				ROOT = { annex_country = { target = PREV } }
			}
			every_state = {
				limit = { 
					NOT = { is_owned_by = ROOT }
					is_on_continent = north_america
				}
				transfer_state_to = ROOT
			}
			recheck_annexations = yes
		}
	}
	check_annexation_SAM = {
		icon = generic_flags

		fire_only_once = no 
		cost = no 

		complete_effect = {
			every_country = {
				limit = { is_in_south_america = yes }
				ROOT = { annex_country = { target = PREV } }
			}
			every_state = {
				limit = { 
					NOT = { is_owned_by = ROOT }
					is_on_continent = south_america
				}
				transfer_state_to = ROOT
			}
			recheck_annexations = yes
		}
	}
	check_annexation_OCN = {
		icon = generic_flags

		fire_only_once = no 
		cost = no 

		complete_effect = {
			every_country = {
				limit = { is_in_australia = yes }
				ROOT = { annex_country = { target = PREV } }
			}
			every_state = {
				limit = { 
					NOT = { is_owned_by = ROOT }
					is_on_continent = australia
				}
				transfer_state_to = ROOT
			}
			recheck_annexations = yes
		}
	}
	check_annexation_CTA = {
		icon = generic_flags

		fire_only_once = no 
		cost = no 

		complete_effect = {
			every_country = {
				limit = { is_central_asian_tag = yes }
				ROOT = { annex_country = { target = PREV } }
			}
			every_state = {
				limit = { is_central_asia = yes }
				transfer_state_to = ROOT
			}
			recheck_annexations = yes
		}
	}
	annexation_CHN = {
		icon = generic_flags

		fire_only_once = no
		cost = no

		visible = {
			ROOT = { is_chinese_tag = yes }
		}

		complete_effect = {
			every_state = {
				limit = { is_china_claim_area = yes }
				transfer_state_to = ROOT 
				if = {
					limit = { ROOT = { is_chinese_tag = yes } }
					THIS = { add_core_of = PREV.PREV }
				}
			}
		}
	}
	balkanize_USA = {
		icon = generic_flags

		fire_only_once = no
		cost = no 

		complete_effect = {
			CLF = {
				every_state = {
					limit = {
						OR = {
							region = 118
							state = 1010
							state = 1011
						}
					}
					transfer_state_to = PREV
				}
			}
			CLB = {
				transfer_state = 965
				transfer_state = 966
				transfer_state = 977
				transfer_state = 978
				transfer_state = 979
				transfer_state = 990
				transfer_state = 991
				transfer_state = 992
				transfer_state = 993
			}
			SNE = {
				transfer_state = 1009
			}
			NEE = {
				every_state = {
					limit = { is_new_england = yes }
					transfer_state_to = PREV
				}
			}
			NAR = {
				every_state = {
					limit = { 
						OR = {
							region = 120
							state = 1005
							state = 1006
							state = 1007
						}	
					}
					transfer_state_to = PREV
				}
			}
			TEX = {
				every_state = {
					limit = { 
						OR = {
							state = 1008
							region = 229
						}
					}
					transfer_state_to = PREV
				}
			}
			HAW = {
				transfer_state = 1019
			}
			CAN = {
				transfer_state = 1018
				every_state = {
					limit = { region = 185 }
					transfer_state_to = PREV
				}
			}
			SCA = {
				transfer_state = 981
				transfer_state = 985
			}
		}
	}

	turn_off_plpc_debug = {
		icon = generic_flags

		visible = {
			has_game_rule = { rule = peace_conference_options option = PLAYER_PRIMACY }
			NOT = { has_global_flag = KR_plpc_turned_off }
		}

		fire_only_once = no
		cost = 0

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete turn_off_plpc_debug"
			custom_effect_tooltip = KR_plpc_disable_tt
			set_global_flag = KR_plpc_turned_off
		}
	}

	turn_on_plpc_debug = {
		icon = generic_flags

		visible = {
			has_game_rule = { rule = peace_conference_options option = PLAYER_PRIMACY }
			has_global_flag = KR_plpc_turned_off
		}

		fire_only_once = no
		cost = 0

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete turn_on_plpc_debug"
			custom_effect_tooltip = KR_plpc_enable_tt
			clr_global_flag = KR_plpc_turned_off
		}
	}

	stt_highlight_states = {
		icon = hand_grab

		allowed = {
			always = no
		}

		available = {
			hidden_trigger = { always = no }
		}

		fire_only_once = no
		days_mission_timeout = 7
		is_good = yes

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout stt_highlight_states"
			hidden_effect = {
				for_each_scope_loop = {
					array = ROOT.marked_states
					clr_state_flag = marked_for_transfer
				}
				clear_array = ROOT.marked_states
				clear_variable = ROOT.update_stt_gui
			}
		}
	}
}

